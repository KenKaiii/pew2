# Screenshots

These are what the README shows. Replace them by dropping new files here with
the same names, nothing else to change:

| File | What it should show |
| --- | --- |
| `01-empty.png` | A fresh conversation: orb, greeting, composer |
| `02-session.png` | An agent mid-task, ideally with the model picker open |

**Take them on a real device**, not the simulator: the status bar, the home
indicator and the real font rendering are most of what makes a screenshot look
like a product rather than a mock.

Capture on iPhone with Side + Volume Up. AirDrop to the Mac, rename, drop in.

Web is not a substitute. `expo-secure-store` is a no-op on web, so the app can
never get past the pairing screen there, and the layout is not the device
layout anyway.
